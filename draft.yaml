models:
  Personaje:
    nombre: string
    nivel: integer
    clase: string
    raza: string
    aliniamiento: string
    historia: text

controllers:
  Personaje:
    resource: api
    validate:
      store:
        nombre: string
        nivel: integer
        clase: string
        raza: string
        aliniamiento: string
        historia: string
      update:
        nombre: string
        nivel: integer
        clase: string
        raza: string
        aliniamiento: string
        historia: string
